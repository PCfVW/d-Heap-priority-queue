# Benchmarks

Performance comparisons for d-ary heap operations across the five language implementations (C++, Go, Rust, TypeScript, Zig). See [`../ROADMAP.md`](../ROADMAP.md) — "v2.6.0 — Instrumentation & Benchmarks".

## Status

| Phase | Description | Status |
|-------|-------------|--------|
| 1 | Test graph corpus, generator, grammar spec | ✅ Done (v2.6.0) |
| 2 | Cross-language instrumentation | 🚧 Planned |
| 3 | Benchmark runners + results | 🚧 Planned (depends on Phase 2) |

## Phase 1 — Test graph corpus

[`graphs.toml`](graphs.toml) declares the canonical benchmark corpus. The graphs are generated by [`scripts/graphgen/`](scripts/graphgen/) and committed to [`../examples/dijkstra/graphs/`](../examples/dijkstra/graphs/). All files conform to the strict JSON subset specified in [`../examples/dijkstra/graphs/GRAMMAR.md`](../examples/dijkstra/graphs/GRAMMAR.md), which lets the C++ example parse them with a small hand-rolled parser ([`../examples/dijkstra/Cpp/graph_parser.h`](../examples/dijkstra/Cpp/graph_parser.h)) instead of pulling in a JSON dependency.

| Name | \|V\| | \|E\| | Kind |
|------|---:|---:|------|
| `small` | 6 | 9 | hand-curated (Network Flows Figure 4.7) |
| `medium_sparse` | 100 | 200 | Erdős–Rényi, \|E\|≈2\|V\| |
| `medium_dense` | 100 | 1000 | Erdős–Rényi, \|E\|≈\|V\|^1.5 |
| `medium_grid` | 100 | 360 | 10×10 lattice (4-connected, bidirected) |
| `large_sparse` | 1000 | 2000 | Erdős–Rényi, \|E\|≈2\|V\| |
| `large_dense` | 1000 | 31623 | Erdős–Rényi, \|E\|≈\|V\|^1.5 |
| `large_grid` | 1024 | 3968 | 32×32 lattice |

Erdős–Rényi graphs are post-processed for connectivity (every vertex reachable from `v0`). Weight range is frozen at `[1, 100]` across the corpus to keep weight-distribution effects out of the cross-bucket comparison. All randomness flows from a single per-graph `seed` through `rand_chacha::ChaCha8Rng`, so generation is fully deterministic — `graphgen verify` re-emits each graph and `diff`s against the committed file; non-zero exit on drift.

```bash
cd benchmarks/scripts/graphgen
cargo run --release -- generate   # rewrite the JSON files
cargo run --release -- verify     # byte-for-byte regression check
```

Each Dijkstra example accepts `--graph=<name>` (default `small`) and `--quiet` (suppress per-vertex output for medium/large graphs). See [`../examples/dijkstra/README.md`](../examples/dijkstra/README.md) for per-language invocation.

## Indicative timings

Median of 5 single-shot runs of `dijkstra --graph=<name> --quiet` per cell, captured 2026-05-08 on the developer's Windows 11 workstation against the v2.6.0 Phase 1 binaries. **These are ballpark only** — single-process invocation including startup, no in-program warmup, no statistical analysis. Phase 3 will replace this section with proper benchmark methodology (warmup, repeated-iteration amortization, hardware/compiler reporting).

### `medium_sparse` (n=100, \|E\|=200)

| arity | TypeScript | Go        | Rust    | Zig     | C++     |
|------:|-----------:|----------:|--------:|--------:|--------:|
| d=2   | 819.9 µs   | (sub-res) | 186.6 µs| 845.7 µs| 130.9 µs|
| d=4   | 861.6 µs   | (sub-res) | 136.0 µs| 755.1 µs| 102.9 µs|
| d=8   | 216.7 µs   | (sub-res) | 126.9 µs| 720.5 µs| 102.1 µs|

### `large_grid` (32×32 lattice, \|V\|=1024, \|E\|=3968)

| arity | TypeScript | Go        | Rust     | Zig       | C++      |
|------:|-----------:|----------:|---------:|----------:|---------:|
| d=2   | 4198.0 µs  | 1500.4 µs | 2535.0 µs| 10822.5 µs| 1466.3 µs|
| d=4   | 3259.7 µs  | 1000.2 µs | 2052.4 µs|  9545.0 µs| 1262.7 µs|
| d=8   | 1642.5 µs  | 1999.0 µs | 1948.2 µs|  9107.4 µs| 1254.8 µs|

**Caveats and to-investigate notes:**

- **Go "(sub-res)"** — Go's `time.Since` on Windows quantizes around ~100 µs in single-shot timing, so the `medium_sparse` runs (which complete well below that) all measured 0.0 µs. The `large_grid` numbers (1500.4 / 1000.2 / 1999.0) also show quantization at 500 µs increments. This is a measurement artifact, not a Go-performance fact; Phase 3's in-program iteration loops will amortize.
- **Zig's `large_grid` is conspicuously higher** than Rust/C++ (~5× slower). The example currently deep-copies every vertex/edge string out of the parsed-JSON arena before running Dijkstra — that allocation pressure may dominate at 1024 vertices. Worth investigating in Phase 2 instrumentation.
- **TypeScript `medium_sparse` non-monotonicity** (d=4 slightly slower than d=2) is JIT noise at the sub-millisecond scale; the trend reverses on `large_grid` and matches the d-ary heap thesis (d=8 fastest).
- **Run-to-run variance** of ~10–20% is typical for these single-shot measurements. The d=8 advantage on `large_grid` (visible in TypeScript, Rust, Zig, C++) survives the noise.

## Phase 2 — Cross-language instrumentation (planned)

Extend the v2.4.0 TypeScript instrumentation pattern to all five languages so each can emit per-operation comparison counts with zero (or near-zero) overhead when disabled. The TypeScript reference is live at the **[Interactive Demo](https://pcfvw.github.io/d-Heap-priority-queue/)** — operation counters, comparison counts, and the d=2/4/8 race mode are exactly the signal Phase 2 will produce in the other four languages.

| Language | Mechanism | Overhead when disabled |
|----------|-----------|------------------------|
| C++ | Template policy class | Zero (inlining) |
| Go | Nil stats pointer | ~1 cycle (nil check) |
| Rust | Generic over `StatsCollector` trait | Zero (monomorphization) |
| TypeScript | Existing implementation (v2.4.0) | — |
| Zig | Comptime bool parameter | Zero (branch elimination) |

Operations to count: `insert`, `pop`, `decreasePriority`, `updatePriority`.

## Phase 3 — Benchmark infrastructure (planned)

Depends on Phase 2 — runners need instrumentation hooks.

- `scripts/<lang>/` — per-language benchmark runners
- `results/` — comparative timing + comparison-count data (d=2 vs d=4 vs d=8 across all 6 corpus graphs)
- `methodology.md` — reproducibility protocol: warm-up runs, median of N trials, hardware/compiler versions, statistical reporting

### Planned comparisons

**Arity** — d=2 (baseline, simpler arithmetic but more `decrease_priority` calls), d=4 (expected sweet spot, shallower tree, cache-friendly), d=8 (`pop` becomes expensive).

**Density** — sparse (`pop` dominates), dense (`decrease_priority` dominates), grid (regular access pattern, baseline).

**Cross-language** — C++ / Rust / Zig (compiled, minimal runtime) vs Go (compiled, GC) vs TypeScript (JIT).

## Directory structure

```
benchmarks/
├── README.md          # this file
├── graphs.toml        # canonical graph corpus declarations
├── scripts/
│   └── graphgen/      # Rust generator + verifier (Phase 1)
└── results/           # populated in Phase 3
```

## Contributing

Want to help shape Phase 2 instrumentation or Phase 3 methodology? Open an issue titled `[Benchmarks]` with your proposal.
